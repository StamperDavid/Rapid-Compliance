========================================
 🚀 AI CRM PLATFORM - QUICK START GUIDE
========================================

📍 Your Location: E:\AI Sales Platform

🎯 TO START THE PROJECT RIGHT NOW:

Step 1: Open PowerShell in this directory
        (Right-click folder → "Open in Terminal")

Step 2: Run ONE of these commands:

   OPTION A - Super Quick (Recommended)
   ────────────────────────────────────
   .\scripts\quick-start.ps1
   
   ✓ Installs dependencies if needed
   ✓ Starts dev server automatically
   ✓ Opens in new window
   ✓ Browser ready at http://localhost:3000


   OPTION B - Full Setup (First Time)
   ───────────────────────────────────
   .\scripts\setup-dev.ps1
   
   ✓ Complete setup
   ✓ Creates all directories
   ✓ Configures environment
   
   Then run:
   .\scripts\quick-start.ps1


   OPTION C - With Extra Services
   ───────────────────────────────
   .\scripts\start-dev.ps1 -All
   
   ✓ Starts Next.js
   ✓ Starts Firebase Emulators
   ✓ Starts Redis (if installed)
   ✓ Multiple service windows


   OPTION D - Manual (Classic)
   ────────────────────────────
   npm install      # First time only
   npm run dev      # Start server

========================================

⚙️ WHAT YOU HAVE:

✅ Complete TypeScript type definitions
   - Organizations, Workspaces, Users
   - Schemas (custom objects)
   - Views (Table, Kanban, Calendar, etc.)
   - AI Agents
   - Workflows
   - E-Commerce
   - Themes with gradients & white-labeling

✅ Core Libraries
   - Schema Manager (create/edit objects)
   - Formula Engine (computed fields)
   - Theme Generator (CSS from config)
   - Widget SDK (embeddable storefronts)

✅ PowerShell Scripts
   - setup-dev.ps1 (full setup)
   - start-dev.ps1 (start services)
   - quick-start.ps1 (fastest start)

✅ Configuration Files
   - package.json (dependencies)
   - tailwind.config.ts (styling)
   - .env.local template (API keys)

========================================

📋 NEXT STEPS AFTER STARTING:

1. Update .env.local with your API keys
   - Firebase keys
   - Gemini API key
   - Stripe keys

2. Access http://localhost:3000

3. Start building:
   - Create workspaces
   - Build custom objects
   - Customize themes
   - Enable e-commerce
   - Train AI agents

========================================

🎨 THEME CUSTOMIZATION:

You can now customize:
✓ Gradients (linear, radial, conic)
✓ Border curves (all components)
✓ Logo swapping (multiple variants)
✓ Glass morphism effects
✓ Colored shadows/glows
✓ Full white-labeling
✓ Custom domains
✓ Remove "Powered by" branding

========================================

🛒 E-COMMERCE FEATURES:

✓ One-click activation
✓ Product mapping from CRM objects
✓ Embeddable widgets
✓ WordPress shortcodes
✓ Full payment processing
✓ Inventory sync
✓ Order management

Example shortcode:
[crm-store id="abc123" type="full"]

========================================

📚 DOCUMENTATION:

- README.md               (Project overview)
- HOW_TO_RUN.md          (Detailed run instructions)
- ARCHITECTURE.md         (System design)
- ECOMMERCE_EMBEDDABLE.md (Widget docs)
- SUMMARY.md             (Implementation status)

========================================

❓ TROUBLESHOOTING:

Issue: "npm is not recognized"
Fix: Install Node.js from https://nodejs.org/

Issue: Port 3000 in use
Fix: Kill process or use: $env:PORT=3001

Issue: Missing dependencies
Fix: Run: npm install

========================================

💻 DEVELOPMENT COMMANDS:

npm run dev          → Start dev server
npm run build        → Production build
npm run lint         → Check code quality
npm run type-check   → TypeScript check
npm test             → Run tests

========================================

🎉 YOU'RE READY!

Just run this command to start:

   .\scripts\quick-start.ps1

Then open: http://localhost:3000

Happy coding! 🚀

========================================


